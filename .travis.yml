language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version
  - docker-compose --version
  - docker-compose build --parallel

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push ahmedsamir89get/reverseproxy
  - docker push ahmedsamir89get/udagram-api-user
  - docker push ahmedsamir89get/udagram-api-feed
  - docker push ahmedsamir89get/udagram-frontend